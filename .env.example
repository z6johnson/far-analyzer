ANTHROPIC_MODEL=claude-sonnet-4-6

# Direct Anthropic API — currently the only active LLM path.
ANTHROPIC_API_KEY=

# LiteLLM proxy path (disabled). Uncomment and restore the commented-out code
# in lib/llm/call.ts, lib/llm/client.ts, and app/api/healthz/route.ts to
# re-enable the proxy route.
# LITELLM_API_KEY=
# LITELLM_BASE_URL=https://tritonai-api.ucsd.edu
